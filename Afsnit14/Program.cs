namespace Afsnit14;

class Program
{
    static void Main(string[] args)
    {
        // Objekt: En instans af en klasse, der har tilstand og adfærd.
        // Fordele ved OOP: Genbrug af kode, bedre struktur, og lettere vedligeholdelse.
        // Klasse: En skabelon for objekter, der definerer tilstand (felter) og adfærd (metoder).
        // Metode: En funktion defineret i en klasse, der beskriver objektets adfærd.
    }
}